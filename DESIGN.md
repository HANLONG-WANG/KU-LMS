# Design

This repository's canonical UI design contract lives in:

- `docs/ku-lms-design-code.md`

All future AI/frontend/UI work in this repository MUST treat that file as the binding design standard unless the user explicitly overrides it.
